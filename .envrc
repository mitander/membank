#!/usr/bin/env bash

# Install project Zig if not present
if [[ ! -x "zig/zig" ]]; then
    echo "Installing project Zig..."
    scripts/install-zig.sh
fi

# Show usage reminder
echo "Use ./zig/zig (not global zig) for this project"
