#!/usr/bin/env bash

# CortexDB direnv configuration
# Installs project Zig and shows usage reminder

# Install project Zig if not present
if [[ ! -x "zig/zig" ]]; then
    echo "Installing project Zig..."
    scripts/install-zig.sh
fi

echo "⚠️  CortexDB: Use ./zig/zig (not global zig) for correct version"
